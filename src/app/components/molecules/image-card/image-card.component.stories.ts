import { moduleMetadata, Story, Meta } from '@storybook/angular';import { ImageCardComponent } from './image-card.component';export default {title: 'ImageCard',component: ImageCardComponent,decorators: [moduleMetadata({declarations: [ImageCardComponent],}),],} as Meta;const Template: Story<ImageCardComponent> = args => ({ props: { ...args }});export const Default = Template.bind({});Default.args = {};